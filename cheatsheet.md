# 📘 DevOps Survival Guide (From Anykey to Engineer)

---

## 🐧 1. Linux & SSH (Фундамент)
**Головна теза:** Мишка — зло. Все робиться через термінал.

### SSH (Безпечний вхід)
*   **Генерація:** `ssh-keygen -t ed25519 -f my_key`
*   **Закинути ключ:** `ssh-copy-id -i my_key.pub user@host` (або `user@IP`)
*   **Зайти:** `ssh -i my_key user@host` (додати `-p 2222`, якщо порт нестандартний)
*   **⚠️ Права доступу (Критично для SSH):**
    *   Папка `~/.ssh` -> **700** (`drwx------`)
    *   Файли ключів -> **600** (`-rw-------`)

### Адміністрування Linux
*   **`sudo`**: "Зроби це як Батько (Адмін)".
*   **`chmod +x file`**: Зробити файл програмою (дозволити запуск).
*   **`chown user:user file`**: Змінити власника файлу.
*   **Права на Docker:**
    *   `sudo usermod -aG docker $USER` — додати себе в групу (щоб без sudo).
    *   `newgrp docker` — оновити групу в поточному вікні.

### Мережа та Процеси (Діагностика)
*   **`ss -tulpn`**: "Покажи, хто зайняв порти" (найважливіше при конфліктах).
*   **`ps aux | grep nginx`**: Знайти процес по імені.
*   **`pgrep -a sleep`**: Швидкий пошук ID процесу.
*   **`kill <PID>`**: Вбити процес по ID.
*   **`curl -v http://localhost:80`**: Перевірити сайт з консолі (як браузер, тільки текст).

---

## 🐙 2. Git (Контроль версій)
**Головна теза:** Немає в Git — не існує.

1.  **`git init`** — старт нового проекту.
2.  **`git add .`** — зібрати всі зміни в "коробку".
3.  **`git commit -m "Опис змін"`** — заклеїти коробку і підписати.
4.  **`git push`** — відправити коробку на хмару.
5.  **`git status`** — перевірка (робити постійно!).

---

## 🐳 3. Docker (Одиничні контейнери)
**Головна теза:** Ізоляція. "У мене працює" — більше не аргумент.

*   **Image (Образ):** Рецепт/Книга на диску. Незмінний.
*   **Container (Контейнер):** Запущена програма в пам'яті.

### Команди
*   **`docker build -t my-app .`** — зібрати образ з Dockerfile.
*   **`docker run -d -p 8080:80 --name my-web my-app`** — запустити.
    *   `-d`: у фоні (detach).
    *   `-p`: порт Зовні:Всередині.
*   **`docker ps`** — список живих.
*   **`docker ps -a`** — список всіх (включно з мертвими).
*   **`docker logs my_app`** — чому воно вмерло? (читати логи).
*   **`docker exec -it my_app bash`** — зайти всередину контейнера (хірургія).
*   **`docker rm -f my_app`** — примусово видалити.

### Dockerfile (Структура)
*   `FROM` (база) -> `WORKDIR` (папка) -> `COPY` (файли) -> `RUN` (інстал) -> `CMD` (старт).

---

## 🎼 4. Docker Compose (Оркестрація локально)
**Головна теза:** Запускаємо групу контейнерів однією командою. Файл `docker-compose.yaml`.

*   **`build: ./folder`** — збираємо самі з файлів.
*   **`image: nginx:latest`** — качаємо готове.
*   **`ports: "3000:80"`** — прокидання портів (Зовні -> Всередині).
*   **`volumes: - ./data:/var/lib/db`** — щоб дані не зникли після видалення контейнера.
*   **`environment:`** — передача паролів і налаштувань.

### Команди
*   **`docker compose up -d`** — підняти все у фоні.
*   **`docker compose down`** — вимкнути і видалити все.
*   **`docker compose restart <service>`** — перезапустити окремий сервіс.

---

## 🛠️ 5. Ansible (Налаштування серверів)
**Головна теза:** Налаштування (Configuration Management). Один скрипт керує сотнею серверів.

*   **Inventory (`hosts.ini`):** Список адрес серверів.
*   **Playbook (`site.yml`):** Інструкція дій.
*   **Команда:** `ansible-playbook -i hosts.ini site.yml`.
*   **Модулі:** `apt` (софт), `copy` (файли), `file` (папки/права), `service` (перезапуск), `user` (створення юзерів).

---

## 🏗️ 6. Terraform (Інфраструктура як Код)
**Головна теза:** Створення ресурсів (IaC). Ми створюємо "залізо" кодом.

*   **Workflow (Свята Трійця):**
    1.  **`terraform init`** — скачати інструменти.
    2.  **`terraform plan`** — показати план (Dry Run).
    3.  **`terraform apply`** — створити ресурси.
*   **`terraform destroy`** — знищити все.
*   **State (`terraform.tfstate`):** Пам'ять про реальність. Не видаляти і не правити руками!

---

## ☸️ 7. Kubernetes (K8s - Оркестрація кластера)
**Головна теза:** Самовилікування та масштабування.

*   **`minikube start`** — розбудити локальний кластер.
*   **`kubectl`** — головний пульт керування.

### Основні об'єкти
*   **Pod:** Найменша одиниця (смертний).
*   **Deployment:** "Прораб". Контролює кількість подів, перезапускає їх.
*   **Service:** "Секретар". Дає стабільну адресу.
    *   `ClusterIP`: Тільки для своїх (всередині).
    *   `NodePort`: Вхід ззовні (через порт на ноді).
*   **PVC:** Диск для бази даних (щоб дані жили вічно).
*   **ConfigMap:** Файл налаштувань (напр. `nginx.conf`).

### Корисні команди
*   **`kubectl get pods`** — статус (Running / Pending / Error).
*   **`kubectl apply -f file.yaml`** — запустити/оновити.
*   **`kubectl delete -f file.yaml`** — видалити.
*   **`minikube service my-service --url`** — отримати посилання для входу (в браузер).
*   **`kubectl port-forward service/<name> 8080:80`** — прорити тунель на свій комп.

### Діагностика (Коли все погано)
*   **`kubectl logs <pod_name>`** — що пише програма?
*   **`kubectl describe pod <pod_name>`** — "Рентген" (чому висить у Pending?).
*   **`kubectl exec -it <pod_name> -- bash`** — зайти всередину пода.
*   **`kubectl rollout restart deployment <name>`** — "Перезавантаж це негайно!" (оновити конфіги).

---

## 🧠 Принципи Інженера
1.  **Не гадай — дивись логи.** (`logs`, `describe`, `/var/log`).
2.  **Не роби руками — пиши скрипт.** (Ansible/Terraform).
3.  **Не хардкодь паролі — юзай змінні (ENV).**
4.  **"It's always DNS"** (або закритий порт). Перевіряй `ping`, `curl`, `nc`.
