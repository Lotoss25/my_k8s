# 📘 DevOps Survival Guide (Ultimate Gold Edition)

---

## 🐧 1. Linux & SSH (Фундамент)
**Головна теза:** Мишка — зло. Все робиться через термінал.

### SSH (Безпечний вхід)
*   **Генерація:** `ssh-keygen -t ed25519 -f my_key`
*   **Закинути ключ:** `ssh-copy-id -i my_key.pub user@host` (або `user@IP`)
*   **Зайти:** `ssh -i my_key user@host` (додати `-p 2222`, якщо порт нестандартний)
*   **⚠️ Права доступу (Критично!):**
    *   Папка `~/.ssh` -> **700** (`drwx------`)
    *   Файли ключів -> **600** (`-rw-------`)

### Адміністрування
*   **`sudo`**: Режим бога.
*   **`chmod +x file`**: Дозволити запуск файлу.
*   **`chown user:user file`**: Змінити власника.
*   **`ps aux | grep name`**: Знайти процес.
*   **`ss -tulpn`**: Хто слухає порти? (Діагностика `Address already in use`).
*   **`curl -v http://localhost`**: Перевірка сайту з консолі.

---

## 🐳 2. Docker (Основи)
*   **`docker build -t my-app .`** — зібрати образ.
*   **`docker run -d -p 8080:80 --name my-web my-app`** — запустити.
*   **`docker ps`** / **`docker ps -a`** — список живих / всіх контейнерів.
*   **`docker logs -f <name>`** — читати логи (чому впало?).
*   **`docker exec -it <name> sh`** — зайти всередину (хірургія).
*   **`docker rm -f <name>`** — примусово видалити.
*   **`docker stats`** — скільки жере пам'яті/CPU.

---

## 🎼 3. Docker Compose (Локальний оркестр)
**Файл:** `docker-compose.yaml`.

*   **`docker compose up -d`** — запустити все у фоні.
*   **`docker compose down`** — видалити все.
*   **`docker compose restart <service>`** — перезапустити окремий сервіс (напр. `web`).
*   **Структура:**
    *   `build: .` (збираємо самі) або `image: nginx` (качаємо).
    *   `ports: "3000:80"` (Зовні:Всередині).
    *   `volumes: - ./data:/var/lib/db` (Дані на диску).
    *   `environment:` (Змінні, паролі).

---

## 🛠️ 4. Ansible (Configuration Management)
**Головна теза:** Налаштовуємо сервери скриптами.

*   **Команда:** `ansible-playbook -i inventory.ini playbook.yml`.
*   **Структура:** `hosts` -> `tasks` -> `module` (apt, copy, file, service, user).
*   **Ідемпотентність:** Можна запускати 100 разів, нічого зайвого не зламається.

---

## 🏗️ 5. Terraform (Infrastructure as Code)
**Головна теза:** Створюємо ресурси (AWS, Docker) кодом.

*   **`terraform init`** — скачати драйвери.
*   **`terraform plan`** — перевірка (що буде створено).
*   **`terraform apply`** — створити.
*   **`terraform destroy`** — знищити все.
*   **LocalStack Hack:** `s3_use_path_style = true` (щоб працювало локально без DNS).

---

## ☸️ 6. Kubernetes (K8s - The Boss)
**Головна теза:** Оркестрація, масштабування, самовилікування.

### Управління кластером (Minikube)
*   **`minikube start`** — увімкнути.
*   **`minikube stop`** — вимкнути (дані зберігаються).
*   **`minikube delete`** — видалити все під нуль.
*   **`minikube image load <image_name>`** — закинути локальний докер-образ у кластер.

### Основні команди (`kubectl`)
*   **`kubectl get pods`** — статус подів (Running/Pending/Error).
*   **`kubectl get svc`** — сервіси (IP та порти).
*   **`kubectl apply -f file.yaml`** — запустити/оновити.
*   **`kubectl delete -f file.yaml`** — видалити.
*   **`kubectl logs <pod_name>`** — дивитися, що пише програма.
*   **`kubectl describe pod <pod_name>`** — "Рентген" (чому висить у Pending?).

### 🕵️‍♂️ Debugging & Networking (Важливо!)
*   **Port-Forward (Тунель):**
    `kubectl port-forward --address 0.0.0.0 service/<name> 8080:80`
    *(Дозволяє зайти з Windows на WSL/Minikube).*
*   **Вхід всередину:** `kubectl exec -it <pod> -- bash`.
*   **Перевірка DNS зсередини:** `nslookup my-service`.
*   **Жорсткий рестарт:** `kubectl rollout restart deployment <name>` (щоб підхопив нові конфіги).

### 📦 Helm (Пакетний менеджер)
*   **`helm repo add ...`** — додати магазин.
*   **`helm install my-app bitnami/wordpress`** — встановити.
*   **`helm uninstall my-app`** — видалити.
*   **Секрети:** `kubectl get secret ... -o jsonpath="{...}" | base64 -d` (дістати пароль).

---

## 🧠 Принципи Інженера (DevOps Mindset)
1.  **Не гадай — дивись логи** (`docker logs`, `kubectl describe`, `/var/log`).
2.  **Не роби руками — пиши код** (Ansible/Terraform/YAML).
3.  **Не хардкодь паролі** — використовуй змінні (ENV) та Secrets.
4.  **Мережа:** Якщо сервіс А не бачить сервіс Б — це або **DNS** (ім'я), або **Port** (закритий), або **IP** (не той інтерфейс `0.0.0.0`).
