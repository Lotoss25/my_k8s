# 📘 DevOps Survival Guide (Platinum Edition)

---

## 🐧 1. Linux & SSH (Фундамент)
**Головна теза:** Мишка — зло. Все робиться через термінал.

### SSH (Безпечний вхід)
*   **Генерація:** `ssh-keygen -t ed25519 -f my_key`
*   **Закинути ключ:** `ssh-copy-id -i my_key.pub user@host`
*   **Зайти:** `ssh -i my_key user@host` (додати `-p 2222`, якщо порт нестандартний)
*   **⚠️ Права доступу (Критично!):**
    *   Папка `~/.ssh` -> **700** (`drwx------`)
    *   Файли ключів -> **600** (`-rw-------`)

### Адміністрування
*   **`sudo`**: Режим бога.
*   **`chmod +x file`**: Дозволити запуск файлу.
*   **`chown user:user file`**: Змінити власника.
*   **`ss -tulpn`**: Хто слухає порти? (Діагностика `Address already in use`).
*   **`curl -v http://localhost`**: Перевірка сайту з консолі.
*   **`ps aux | grep name`**: Знайти процес.
*   **`kill <PID>`**: Вбити процес.

---

## 🐙 2. Git (Контроль версій)
**Головна теза:** Немає в Git — не існує.

### Базові команди
1.  **`git init`** — старт.
2.  **`git status`** — перевірка (робити постійно!).
3.  **`git add .`** — додати все в "коробку".
4.  **`git commit -m "Текст"`** — заклеїти коробку.
5.  **`git push`** — відправити на хмару.

### 🛑 .gitignore (Що НЕ МОЖНА комітити)
Створи файл `.gitignore` в корені і впиши туди:
*   **Для Terraform:** `.terraform/`, `*.tfstate`, `*.tfstate.backup`, `.terraform.lock.hcl`
*   **Для Python:** `__pycache__/`, `venv/`, `.env` (паролі!)
*   **Для System:** `.DS_Store`

### 🚑 Git Troubleshooting (Якщо накосячив)
**Ситуація:** "File too large (100MB)" або додав не той файл, але ще не запушив.
1.  **`git reset --soft HEAD~1`** — Скасувати останній коміт (файли залишаться, але перестануть бути "закоміченими").
2.  **`git reset`** — Прибрати все з індексу (unstaged).
3.  Додай сміття в `.gitignore`.
4.  Роби `git add .` заново.

**Ситуація:** "fatal: pathspec ... is in submodule".
1.  `rm -rf folder/.git` (видалити гіт всередині папки).
2.  `git rm --cached folder` (забути папку).
3.  `git add folder` (додати як звичайну папку).

---

## 🐳 3. Docker (Основи)
**Головна теза:** Ізоляція.

*   **`docker build -t my-app .`** — зібрати образ.
*   **`docker run -d -p 8080:80 --name my-web my-app`** — запустити.
*   **`docker ps`** / **`docker ps -a`** — список живих / всіх.
*   **`docker logs -f <name>`** — читати логи (чому впало?).
*   **`docker exec -it <name> sh`** — зайти всередину.
*   **`docker rm -f <name>`** — примусово видалити.
*   **`docker stats`** — моніторинг ресурсів.

---

## 🎼 4. Docker Compose (Локальний оркестр)
**Файл:** `docker-compose.yaml`.

*   **`docker compose up -d`** — підняти все у фоні.
*   **`docker compose down`** — видалити все.
*   **`docker compose restart <service>`** — перезапустити сервіс.
*   **Структура:**
    *   `build: .` або `image: nginx`
    *   `ports: "3000:80"` (Зовні:Всередині)
    *   `volumes: - ./data:/var/lib/db` (Дані на диску)
    *   `environment:` (Змінні, паролі)

---

## 🛠️ 5. Ansible (Configuration Management)
**Головна теза:** Налаштовуємо сервери скриптами.

*   **Команда:** `ansible-playbook -i inventory.ini playbook.yml`.
*   **Ідемпотентність:** `state: present` (якщо вже є — не чіпай, якщо нема — постав).
*   **Модулі:** `apt`, `copy`, `file`, `service`, `user`, `get_url`.

---

## 🏗️ 6. Terraform (Infrastructure as Code)
**Головна теза:** Створюємо ресурси (AWS, Docker) кодом.

*   **`terraform init`** — ініціалізація (качає важкі плагіни!).
*   **`terraform plan`** — перевірка плану.
*   **`terraform apply`** — створення.
*   **`terraform destroy`** — знищення.
*   **⚠️ ВАЖЛИВО:** Ніколи не коміть файл `terraform.tfstate`! Там зберігаються всі паролі відкритим текстом. Додай його в `.gitignore`.

---

## ☸️ 7. Kubernetes (K8s - The Boss)
**Головна теза:** Оркестрація, масштабування, самовилікування.

*   **`minikube start`** — старт кластера.
*   **`kubectl get pods`** — статус подів.
*   **`kubectl apply -f file.yaml`** — запустити/оновити.
*   **`kubectl delete -f file.yaml`** — видалити.
*   **`kubectl logs <pod>`** — логи.
*   **`minikube service <name> --url`** — отримати лінк.
*   **`kubectl port-forward service/<name> 8080:80`** — тунель на свій комп.
*   **Helm:** `helm install my-app bitnami/wordpress` (пакетний менеджер).

---

## 🧠 Принципи Інженера
1.  **Не гадай — дивись логи** (`docker logs`, `kubectl logs`, `/var/log`).
2.  **Перед комітом — перевір `.gitignore`.**
3.  **Не роби руками — пиши код** (Ansible/Terraform/YAML).
4.  **"It's always DNS"** (або закритий порт). Перевіряй `ping`, `curl`, `nc`.
