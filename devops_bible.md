# 📘 DevOps Survival Guide (From Anykey to Engineer)

## 🐧 1. Linux & SSH (Фундамент)
**Головна теза:** Мишка — зло. Все робиться через термінал.

### SSH (Безпечний вхід)
*   **Генерація:** `ssh-keygen -t ed25519 -C "comment"`
*   **Закинути ключ:** `ssh-copy-id -i ~/.ssh/my_key.pub user@ip`
*   **Зайти:** `ssh -i ~/.ssh/my_key user@ip`
*   **Конфіг:** `~/.ssh/config` (аліаси для хостів).

### Права доступу
*   **`chmod 700 ~/.ssh`** — папка (тільки я).
*   **`chmod 600 ~/.ssh/id_rsa`** — приватний ключ (тільки я).
*   **`chmod +x file`** — дозвіл на запуск.
*   **`chown user:user file`** — змінити власника.
*   **`sudo`** — режим бога.

### Мережа та Процеси
*   **`ss -tulpn`** — хто слухає порти?
*   **`ps aux | grep nginx`** — знайти процес.
*   **`kill <PID>`** — вбити процес.
*   **`curl -v http://localhost`** — перевірити сайт.

---

## 🐙 2. Git (Контроль версій)
**Головна теза:** Немає в Git — не існує.

*   **`git init`** — старт.
*   **`git status`** — перевірка.
*   **`git add .`** — додати все.
*   **`git commit -m "Text"`** — зберегти.
*   **`git push`** — відправити.
*   **`.gitignore`** — що ігнорувати.

---

## 🐳 3. Docker (Контейнеризація)
**Головна теза:** "У мене працює" — не аргумент.

*   **`docker build -t my-app .`** — зібрати образ.
*   **`docker run -d -p 8080:80 --name my-web my-app`** — запустити.
*   **`docker ps`** — список живих.
*   **`docker logs -f <name>`** — дивитися логи.
*   **`docker exec -it <name> sh`** — зайти всередину.
*   **`docker rm -f <name>`** — видалити.

### Dockerfile
`FROM` (база) -> `WORKDIR` (папка) -> `COPY` (файли) -> `RUN` (інстал) -> `CMD` (старт).

---

## 🎼 4. Docker Compose (Оркестрація локально)
**Головна теза:** Описуємо інфраструктуру у `docker-compose.yaml`.

*   **`docker compose up -d`** — підняти все.
*   **`docker compose down`** — видалити все.
*   **`docker compose restart <service>`** — перезапустити сервіс.
*   **Volumes:** Зберігають дані після смерті контейнера.

---

## 🛠️ 5. Ansible (Налаштування серверів)
**Головна теза:** Один скрипт керує сотнею серверів.

*   **Inventory (`hosts.ini`):** Список адрес.
*   **Playbook (`site.yml`):** Інструкція YAML.
*   **Команда:** `ansible-playbook -i hosts.ini site.yml`.
*   **Ключове:** Ідемпотентність (можна запускати багато разів безпечно).

---

## 🏗️ 6. Terraform (Інфраструктура як Код)
**Головна теза:** Створюємо ресурси кодом.

*   **`terraform init`** — ініціалізація.
*   **`terraform plan`** — перевірка (що буде створено).
*   **`terraform apply`** — створення.
*   **`terraform destroy`** — знищення.
*   **State (`terraform.tfstate`):** Пам'ять про реальність. Не видаляти!

---

## ☸️ 7. Kubernetes (K8s - Оркестрація кластера)
**Головна теза:** Самовилікування та масштабування.

*   **`minikube start`** — старт кластера.
*   **`kubectl get pods`** — список подів.
*   **`kubectl apply -f file.yaml`** — запустити/оновити.
*   **`kubectl logs <pod>`** — логи.
*   **Pod:** Обгортка для контейнера (смертний).
*   **Deployment:** Контролює кількість подів (безсмертний).
*   **Service:**
    *   *ClusterIP:* Внутрішній зв'язок (Python <-> DB).
    *   *NodePort:* Вхід ззовні (Nginx).
*   **ConfigMap:** Конфіги окремо від коду.
*   **PVC:** Диск для бази даних.

---

## 🧠 Принципи Інженера
1.  **Не гадай — дивись логи.**
2.  **Не роби руками — пиши скрипт.**
3.  **Не хардкодь паролі — юзай змінні (ENV).**
4.  **"It's always DNS"** (або закритий порт).
