# 📘 DevOps Survival Guide (Ultimate Edition)

## 🐧 1. SSH (Доступ)
**Головна теза:** Вхід без пароля — це база безпеки.

*   **Генерація:** `ssh-keygen -t ed25519 -f my_key` (створити іменований ключ).
*   **Копіювання:** `ssh-copy-id -i my_key.pub user@host` (закинути на сервер).
*   **Вхід:** `ssh -i my_key user@host` (або `ssh -p 2222 ...` якщо нестандартний порт).
*   **⚠️ Права доступу (Це святе!):**
    *   Папка `~/.ssh` -> **700** (`drwx------`) — тільки я можу зайти.
    *   Файли ключів -> **600** (`-rw-------`) — тільки я можу читати.

---

## 💻 2. Linux (База адміна)
*   **`sudo`**: "Зроби це як Батько (Адмін)".
*   **`chmod +x file`**: Зробити файл програмою (дозволити запуск).
*   **`chown user:user file`**: Змінити власника файлу.
*   **`ss -tulpn`**: "Покажи, хто зайняв порти" (найважливіше при конфліктах).
*   **Робота з процесами:**
    *   `ps aux | grep nginx`: Класика пошуку процесів.
    *   `pgrep -a sleep`: Швидкий пошук PID по імені.
    *   `kill <PID>`: Вбити процес.
*   **Права на Docker (щоб без sudo):**
    *   `sudo usermod -aG docker $USER`: Додати себе в групу.
    *   `newgrp docker`: Застосувати групу без перезавантаження.

---

## 🐳 3. Docker (Одиничні контейнери)
*   **Образ (Image):** Рецепт/Книга на диску. Незмінний.
*   **Контейнер:** Запущена програма в пам'яті.
*   **`docker build -t my-app .`** — зібрати образ.
*   **`docker run -d -p 8080:80 --name my-web my-app`** — запустити.
*   **`docker ps`** — що працює зараз?
*   **`docker ps -a`** — що працювало і вмерло?
*   **`docker logs my_app`** — чому воно вмерло? (читати логи).
*   **`docker exec -it my_app bash`** — зайти всередину контейнера (хірургія).
*   **`docker rm -f my_app`** — примусово видалити.

---

## 🎼 4. Docker Compose (Оркестр)
**Файл:** `docker-compose.yaml` (ноти для оркестру).

*   **build:** `./folder` — збираємо самі з файлів.
*   **image:** `nginx:latest` — качаємо готове.
*   **ports:** `"3000:80"` — Зовні 3000 -> Всередині 80.
*   **volumes:** `- ./data:/var/lib/mysql` — щоб дані не зникли після видалення.
*   **environment:** `- DB_PASS=123` — передача налаштувань.
*   **Команди:**
    *   `docker compose up -d` — запустити все у фоні.
    *   `docker compose down` — вимкнути і видалити все.
    *   `docker compose restart <service>` — перезапустити окремий сервіс.

---

## 🐙 5. Git (Машина часу)
1.  `git add .` — "Збери всі зміни в коробку".
2.  `git commit -m "Name"` — "Заклей коробку і підпиши".
3.  `git push` — "Відправ коробку на хмару".
4.  `git status` — перевірка стану (робити постійно!).

---

## ☸️ 6. Kubernetes (K8s - Вища ліга)
**Головна теза:** Самовилікування та масштабування.

*   **Старт:** `minikube start` (розбудити кластер).
*   **Основні команди:**
    *   `kubectl get pods` — список подів (статус Running/Pending/Error).
    *   `kubectl apply -f file.yaml` — запустити/оновити конфіг.
    *   `kubectl delete -f file.yaml` — видалити.
*   **Діагностика (Debugging):**
    *   `minikube service my-nginx-service --url` — отримати посилання для входу.
    *   `kubectl exec -it <pod_name> -- bash` — зайти всередину пода.
    *   `kubectl describe pod <pod_name>` — "Рентген" (чому висить у Pending?).
    *   `kubectl logs <pod_name>` — що пише програма.
    *   `kubectl port-forward service/<name> 8080:80` — тунель на свій комп.
*   **Ремонт:**
    *   `kubectl rollout restart deployment <name>` — "Перезавантаж це негайно!" (оновити конфіги).

---

## 🧠 Принципи Інженера
1.  **Не гадай — дивись логи (`logs`, `describe`).**
2.  **Не роби руками — пиши скрипт (Ansible/Terraform).**
3.  **Не хардкодь паролі — юзай змінні (ENV/Secrets).**
4.  **"It's always DNS"** (або закритий порт). Перевіряй `ping`, `curl`, `nc`.
