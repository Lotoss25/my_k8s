# 1. БАЗА (Основа)
# Беремо найлегшу версію Linux у світі (Alpine), вона важить 5 Мб.
FROM alpine:latest

# 2. РОБОЧЕ МІСЦЕ (Стіл)
# Створюємо папку /app всередині коробки і заходимо в неї.
WORKDIR /app

# 3. ПАКУВАННЯ (Копіювання)
# Беремо файл script.sh з твого компа (ліворуч)
# і кладемо його в коробку (праворуч, тобто в папку /app).
COPY script.sh .

# 4. ПІДГОТОВКА
# Робимо файл виконуваним (всередині коробки).
RUN chmod +x script.sh

# 5. ЗАПУСК (Коли відкриють коробку)
# Кажемо: "Запусти цей скрипт, як тільки контейнер стартує".
CMD ["./script.sh"]
